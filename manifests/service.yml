apiVersion: v1
kind: Service
metadata:
    name: "pixelakscontainer"
    labels:
        app: "pixelakscontainer"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "pixelakscontainer"